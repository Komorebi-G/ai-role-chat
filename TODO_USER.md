你现在是这个项目的长期维护 agent。

目标：
请你先完整分析当前项目的功能、目录结构、技术栈和已有实现，然后创建或更新 todo.md。

todo.md 需要包含：
1. 已有功能总结
2. 当前缺失功能
3. 可扩展方向
4. 按优先级排序的任务列表
5. 每个任务的实现难度、影响范围、涉及文件
6. 已完成任务记录
7. 待确认问题

工作方式：
你需要进入一个迭代循环，但每一轮只执行 1 个最有价值的任务，我的目的是把这个项目完善成可商用级别的，所以各种功能需要十分齐全，比如设置、切换角色、开启新聊天、创建角色卡。注意，你刚刚执行的1 2 4轮对于用户来说都没太大区别，先不用管安全方面，把用户体验感拉上来。

每一轮流程如下：
1. 阅读 todo.md 和项目源码
2. 选择当前优先级最高、范围清晰的一个任务
3. 简要说明你准备修改哪些文件
4. 实现该任务
5. 运行必要的检查，例如 npm run build、npm test、lint，若项目没有这些命令则先阅读 package.json 判断可用命令
6. 修复出现的问题
7. 更新 todo.md：
   - 标记已完成内容
   - 记录修改了什么
   - 记录测试结果
   - 追加新发现的问题
   - 说明这个功能如何使用
8. 提交本轮总结，然后继续下一轮

重要约束：
- 不要一次性大改架构
- 不要删除已有功能，除非明确确认
- 不要凭空引入复杂依赖
- 不要改动 .env、密钥、token
- 不要把 API key 写进代码
- 不确定的设计写入 todo.md 的“待确认问题”
- 每轮修改尽量小而完整
- 如果连续两轮构建失败且无法修复，停止并总结问题
- 如果发现需求不明确，先写入 todo.md，不要强行实现
- 每轮都必须保证 todo.md 是最新的

最多连续执行 10 轮迭代。完成后停止，输出总结、已完成任务、剩余任务和下一步建议。
此文件不要变动，每次执行完之后都把下面的计数器加一，直到5再跟我汇报。
当前已执行10轮。